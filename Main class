
import java.util.ArrayList;
import java.util.Scanner;


public class Main {

    
    
    public static void main(String[] args) {
        
        Scanner scanner = new Scanner(System.in);
        
        ArrayList<Files> ddt = Files.readAndSort("ddt2.CSV");
        ArrayList<Files> giac = Files.readAndSort("Giacenza.CSV");
        
        /*while (true) {
            System.out.println("Scegli il file da stampare");
            String file = scanner.nextLine();
            if (file.isEmpty()) {
                break;
            }
            if (file.equalsIgnoreCase("ddt2")) {
                for (Files d : ddt) {
                    System.out.println(d);
                }                        
            } else if (file.equalsIgnoreCase("giacenza")) {
                for (Files g : giac) {
                    System.out.println(g);
                }
            }
            
        }*/
        ArrayList<Files> orderIt = Files.toOrder(ddt, giac);
        for (Files o : orderIt) {
            System.out.println(o);
        }
    }
    
}
